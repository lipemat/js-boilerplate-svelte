.share {
	composes: shared from '../content.pcss';
	font-weight: 700;

	a {
		background: transparent;
		border-bottom: 1px solid var(--c-yellow);
		border-right: 2px solid var(--c-black);
		padding: 15px;
	}
}
